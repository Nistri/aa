apiVersion: v1
kind: Service
metadata:
  name: mongo-service
  labels:
    app: mongo-service
spec:
  type: NodePort
  ports:
    - port: 27017
      targetPort: mongo-port
      protocol: TCP
  selector:
      app: mymongo-app
