apiVersion: v1
kind: Service
metadata:
  name: web-service
  labels:
    app: web-service
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: webserver-port
      protocol: TCP
  selector:
      app: myweb-app
